# Endpoint que recibe el formulario de contacto (POST JSON { contacto }).
# Si queda vacío, el envío se simula localmente.
VITE_CONTACT_ENDPOINT=

# Supabase (panel de gestión en /admin). Se obtienen en Project Settings → API.
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
